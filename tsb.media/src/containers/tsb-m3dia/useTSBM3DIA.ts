import React, { useEffect, useState, useContext } from "react";
import {
  generateWallet,
  generateSecretKey,
  getStxAddress,
} from "@stacks/wallet-sdk";
import { TransactionVersion } from "@stacks/transactions";

export const useTSBM3DIA = () => {
  return {
    // handleSecretKey,
    // handleConnect,
  };
};
