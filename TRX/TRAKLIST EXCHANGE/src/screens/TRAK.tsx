import {View, Text} from 'react-native';
import React from 'react';
import {TRAKContainer} from '../containers';

export const TRAKScreen = ({...props}: any) => {
  return <TRAKContainer {...props} />;
};
