import {View, Text} from 'react-native';
import React from 'react';
import {ListsContainer} from '../containers';

export const ListsScreen = (props: any) => {
  return <ListsContainer {...props} />;
};
