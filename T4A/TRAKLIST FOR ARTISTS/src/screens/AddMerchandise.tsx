import {View, Text} from 'react-native';
import React from 'react';
import {AddMerchandiseContainer} from '../container';

export const AddMerchandiseScreen = ({...props}) => {
  return <AddMerchandiseContainer {...props} />;
};
