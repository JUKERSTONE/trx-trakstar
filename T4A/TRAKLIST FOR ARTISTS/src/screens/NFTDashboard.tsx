import {View, Text} from 'react-native';
import React from 'react';
import {NFTDashboardContainer} from '../container';

export const NFTDashboardScreen = ({...props}) => {
  return <NFTDashboardContainer {...props} />;
};
