import {View, Text} from 'react-native';
import React from 'react';
import {NFTMerchandiseContainer} from '../container';

export const NFTMerchandiseScreen = ({...props}) => {
  return <NFTMerchandiseContainer {...props} />;
};
