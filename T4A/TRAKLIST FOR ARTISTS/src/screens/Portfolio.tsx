import {View, Text} from 'react-native';
import React from 'react';
import {PortfolioContainer} from '../container';

export const PortfolioScreen = ({...props}) => {
  return <PortfolioContainer {...props} />;
};
