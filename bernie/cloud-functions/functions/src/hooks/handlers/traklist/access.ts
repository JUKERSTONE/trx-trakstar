// import { db, trx } from "../../../firestore";
// import firebase from "firebase";

// export const getTRAKLISTAccess = (req: any, res: any) => {
//   const { email, password } = req.body;

//   firebase.initializeApp(config);

//   trx.auth().

// };
