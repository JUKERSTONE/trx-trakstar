import {View, Text} from 'react-native';
import React from 'react';
import {FindContainer} from '../containers';

export const FindScreen = ({...props}) => {
  return <FindContainer {...props} />;
};
