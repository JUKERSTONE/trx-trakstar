import {View, Text} from 'react-native';
import React from 'react';
import {TRX00MatchContainer} from '../containers';

export const TRX00MatchScreen = ({...props}) => {
  return <TRX00MatchContainer {...props} />;
};
