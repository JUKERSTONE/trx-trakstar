import React from 'react';
import {View, Text, SafeAreaView, TextInput, Button} from 'react-native';
import {RecordsShopContainer} from '../containers';

export const RecordsShopScreen = ({...props}) => {
  return <RecordsShopContainer {...props} />;
};
