import React from 'react';
import {View, Text, SafeAreaView, TextInput, Button} from 'react-native';
import {TRXRequestsContainer} from '../containers';

export const TRXRequestsScreen = ({...props}) => {
  return <TRXRequestsContainer {...props} />;
};
