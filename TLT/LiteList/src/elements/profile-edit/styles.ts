import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    padding: 10,
    backgroundColor: '#FFFFFF',
    borderRadius: 8,
  },
  inputContainer: {
    margin: 5,
  },
});
