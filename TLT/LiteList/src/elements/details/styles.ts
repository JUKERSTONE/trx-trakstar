import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    padding: 10,
    backgroundColor: '#FFFFFF',
    borderRadius: 5,
  },
  inputContainer: {
    margin: 5,
  },
});
