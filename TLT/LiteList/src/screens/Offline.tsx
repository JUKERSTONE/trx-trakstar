import React from 'react';
import {View, Button} from 'react-native';
import {OfflineContainer} from '../containers';

export const OfflineScreen = ({...props}) => {
  return <OfflineContainer {...props} />;
};
