import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  registration_form: {},
  section: {
    marginBottom: 20,
  },
  action: {
    margin: 10,
    alignItems: 'center',
  },
});

export default styles;
