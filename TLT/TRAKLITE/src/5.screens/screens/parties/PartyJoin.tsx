import React from 'react';
import {View, Text, TextInput, Button} from 'react-native';
import {PartyJoinView} from '../../../6.containers';

export const PartyJoin = ({...props}) => {
  return <PartyJoinView {...props} />;
};
