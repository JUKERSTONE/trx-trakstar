import React from 'react';
import {View, Text, TextInput, Button} from 'react-native';
import {PartyStartView} from '../../../6.containers';

export const PartyStart = ({...props}) => {
  return <PartyStartView {...props} />;
};
